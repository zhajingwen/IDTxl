# 加密货币网络分析系统使用指南

## 🎯 项目概述

基于IDTxl的加密货币市场网络分析系统，用于识别高度关联的资产组合。该系统从Hyperliquid API获取实时数据，使用信息论方法分析资产间的关联性，并生成可视化报告。

## 🚀 快速开始

### 1. 环境准备

```bash
# 安装依赖
pip install -r requirements_crypto.txt

# 或者安装IDTxl（如果尚未安装）
pip install idtxl
```

### 2. 基本使用

```bash
# 使用默认配置运行
python run_crypto_analysis.py

# 使用自定义参数
python run_crypto_analysis.py --tokens 30 --hours 72 --correlation 0.8

# 指定输出目录
python run_crypto_analysis.py --output my_analysis_results
```

### 3. 高级使用

```python
# 在Python代码中使用
from crypto_network_analysis import CryptoNetworkAnalyzer

# 创建分析器
analyzer = CryptoNetworkAnalyzer()

# 获取数据
analyzer.fetch_and_preprocess_data()

# 执行分析
analyzer.analyze_network()
results = analyzer.identify_highly_correlated_assets()

# 生成报告
analyzer.visualize_results(results)
report = analyzer.generate_report(results)
```

## 📊 功能特性

### 1. 数据获取
- **API来源**: Hyperliquid开放API
- **数据类型**: 实时价格数据
- **时间窗口**: 可配置（默认7天）
- **代币过滤**: 自动过滤低质量代币

### 2. 网络分析
- **传递熵分析**: 识别信息流动方向
- **相关性分析**: 发现同步变动的资产
- **统计检验**: FDR校正的显著性检验
- **多变量分析**: 同时分析多个资产关系

### 3. 结果识别
- **高相关资产对**: 相关系数超过阈值的资产对
- **传递熵连接**: 信息流动显著的连接
- **资产组合**: 基于相关性和传递熵的组合
- **强度分级**: 高、中、低强度分类

### 4. 可视化报告
- **相关性热力图**: 资产间相关性矩阵
- **网络图**: 传递熵网络结构
- **排名图表**: 高相关对和传递熵连接
- **分布图表**: 组合类型和大小分布

## ⚙️ 配置说明

### 配置文件 (config_crypto.json)

```json
{
  "data_processing": {
    "max_tokens": 50,           // 最大分析代币数量
    "time_hours": 168,          // 分析时间窗口(小时)
    "correlation_threshold": 0.7, // 相关性阈值
    "te_threshold": 0.1         // 传递熵阈值
  },
  "network_analysis": {
    "cmi_estimator": "JidtGaussianCMI", // 估计器类型
    "max_lag_sources": 12,      // 最大源滞后
    "n_perm_max_stat": 100      // 最大统计置换次数
  }
}
```

### 命令行参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--config` | 配置文件路径 | config_crypto.json |
| `--output` | 输出目录 | output |
| `--tokens` | 最大代币数量 | 20 |
| `--hours` | 时间窗口(小时) | 168 |
| `--correlation` | 相关性阈值 | 0.7 |
| `--te` | 传递熵阈值 | 0.1 |

## 📈 输出结果

### 1. 可视化图表 (crypto_network_analysis.png)
- 6个子图的综合分析面板
- 相关性热力图
- 传递熵网络图
- 高相关资产对排名
- 传递熵连接排名
- 组合类型分布
- 组合大小分布

### 2. 分析报告 (crypto_network_report.md)
- 数据概览
- 高相关资产对列表
- 传递熵连接列表
- 识别出的资产组合
- 投资建议

### 3. 结果数据 (analysis_results.json)
- 完整的分析结果数据
- 可用于进一步分析
- JSON格式，便于程序处理

### 4. 日志文件 (crypto_network_analysis.log)
- 详细的执行日志
- 错误信息和调试信息
- 性能统计

## 🔧 高级配置

### 1. 性能优化

```python
# 减少计算时间
config = {
    'network_analysis': {
        'n_perm_max_stat': 50,    # 减少置换次数
        'max_lag_sources': 6,     # 减少滞后范围
    }
}

# 启用GPU加速
config = {
    'network_analysis': {
        'cmi_estimator': 'OpenCLKraskovCMI',
    }
}
```

### 2. 数据过滤

```python
# 自定义代币过滤
def custom_token_filter(token_info):
    return (token_info.get('maxLeverage', 0) > 10 and 
            token_info.get('name', '').startswith('BTC'))

analyzer = CryptoNetworkAnalyzer()
analyzer.data_fetcher._filter_tokens = custom_token_filter
```

### 3. 结果后处理

```python
# 自定义组合识别算法
def custom_combination_finder(correlated_pairs, te_connections):
    # 实现自定义的组合识别逻辑
    pass

analyzer._find_asset_combinations = custom_combination_finder
```

## 📊 使用案例

### 案例1: 快速市场扫描
```bash
# 分析前20个代币，24小时数据
python run_crypto_analysis.py --tokens 20 --hours 24
```

### 案例2: 深度关联分析
```bash
# 分析50个代币，7天数据，高相关性阈值
python run_crypto_analysis.py --tokens 50 --hours 168 --correlation 0.8
```

### 案例3: 实时监控
```bash
# 每小时运行一次，监控市场变化
while true; do
    python run_crypto_analysis.py --output "monitor_$(date +%Y%m%d_%H%M%S)"
    sleep 3600
done
```

## ⚠️ 注意事项

### 1. 数据质量
- API数据可能存在延迟
- 某些代币可能数据不完整
- 建议定期验证数据质量

### 2. 计算资源
- 大规模分析需要较多内存
- 建议在性能较好的机器上运行
- 可考虑使用GPU加速

### 3. 结果解释
- 相关性不等于因果关系
- 历史数据不代表未来表现
- 建议结合其他分析方法

### 4. 风险提示
- 本分析仅供研究参考
- 不构成投资建议
- 投资有风险，请谨慎决策

## 🐛 故障排除

### 常见问题

1. **API连接失败**
   - 检查网络连接
   - 确认API服务状态
   - 增加重试次数

2. **内存不足**
   - 减少代币数量
   - 缩短时间窗口
   - 增加系统内存

3. **计算时间过长**
   - 减少置换次数
   - 使用GPU加速
   - 减少滞后参数

4. **结果为空**
   - 降低相关性阈值
   - 增加时间窗口
   - 检查数据质量

### 调试模式

```python
import logging
logging.basicConfig(level=logging.DEBUG)

# 启用详细日志
analyzer = CryptoNetworkAnalyzer()
analyzer.config['output']['log_level'] = 'DEBUG'
```

## 📚 扩展开发

### 1. 添加新的数据源
```python
class CustomDataFetcher:
    def get_token_prices(self, tokens, hours):
        # 实现自定义数据获取逻辑
        pass

analyzer.data_fetcher = CustomDataFetcher()
```

### 2. 自定义分析算法
```python
class CustomAnalyzer(CryptoNetworkAnalyzer):
    def identify_highly_correlated_assets(self):
        # 实现自定义识别算法
        pass
```

### 3. 添加新的可视化
```python
def custom_visualization(results, save_path):
    # 实现自定义可视化逻辑
    pass

analyzer.visualize_results = custom_visualization
```

## 📞 技术支持

如有问题或建议，请：
1. 查看日志文件获取详细错误信息
2. 检查配置文件是否正确
3. 确认依赖包版本兼容性
4. 参考IDTxl官方文档

---

*本系统基于IDTxl项目开发，用于加密货币市场网络分析。请根据实际需求调整参数和配置。*